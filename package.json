{
  "name": "spectre",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ gogoose/spectre",
    "create-local": "graph create --node http://localhost:8020/ gogoose/spectre",
    "remove-local": "graph remove --node http://localhost:8020/ gogoose/spectre",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 gogoose/spectre"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.19.0",
    "@graphprotocol/graph-ts": "0.19.0"
  }
}
